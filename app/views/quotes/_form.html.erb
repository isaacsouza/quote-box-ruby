<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phrase %>
    <%= form.text_area :phrase, id: :quote_phrase %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :quote_author %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :quote_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
